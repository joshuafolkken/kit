## `epicrun` — EPIC 配下の子 Issue を無人で実行する

**この規則の単一ソースは [`.claude/skills/workflow-commands/epicrun.md`](../../.claude/skills/workflow-commands/epicrun.md) である。** ここに本文を複製しない（「クローン禁止・単一ソース化」の適用。joshuafolkken/kit#1174 のパイロットに続く横展開 joshuafolkken/kit#1188）。

`residency.md` が定めるとおり、`epicrun` の手順は `epicrun` が始まった後にだけ効くので、手順本体は skill 側に置く。かつては同じ内容の日本語全文（307 行 / 49KB）がここにも重複しており、しかも**この節の冒頭がその重複を明文で命じていた** — 実行手順の正典はここであり運用手順は skill に置く、そして両者の内容は揃っていなければならない、という一文である。1 つの規則を直すのに 2 か所へ同じ内容を書かせていたのはその一文であり、縮小にあたって削除した。**この説明も原文を引き写さない** — 引用した時点でその一文はこのファイルに残ることになり、削除したという記述と食い違う（`epicrun-document-rule.test.ts` がこれを検査する）。

**この話題ファイルは全体が 1 話題だったので、節の切り出しは要らずその場で縮小している**（joshuafolkken/kit#1186 が定めた「1 ファイルに複数の話題があるときは節を切り出してから縮小する」に該当しない側）。

参照文書だけにあったのは次の 5 件で、いずれも縮小の前に手順書へ畳み込み済みである。

**内容ではなく所在を書く。** ここは畳み込みが漏れていないかを後から確かめるための索引であり、要約であっても本文を書けば複製になる。

- **`queue` との違い** — 停止が波及する範囲（セッション全体か、子 1 件か）
- **`auto-ok` の拾い上げと依存関係**（joshuafolkken/kit#996）— 前提が未解決の Issue を渡さない理由
- **「同じ順序」と「同じ集合」の差** — 拾い上げが `🗒 Next issues` の先頭を拒むことがある理由
- **待機・終了の判定表の最終行** — open な子が無いときに何をしてから終わるか
- **循環依存の担当分け** — 詰みを解くのが `epicrun` の仕事ではないこと

手順書に置かれている内容:

- EPIC でない素の Issue も受け取ること、その経路で前提・分割が出ても停止せず起票して続けること、何も出てこなければ EPIC を作らないこと
- 1 回の起動が承認する範囲（EPIC 内のマージ・複数リポジトリへの push・実行中の自動起票。外側は `auto-ok` だけ）
- 子 1 件を隔離された実行単位へ委譲すること、親が読むのは要約ではなく `pnpm josh issue:state` が返す GitHub の状態であること
- 並行度モデル（repo あたり同時 1 件・repo 間は並行・`epic:next` が適用する repo 単位の排他・advisory であってアトミックではないこと・90 分の stale 窓）
- `josh latest` はセッションごとに 1 回で、子ごとには走らせないこと
- park and continue（判断が要る子だけを脇に置く）と、`needs-human-review` だけがランを終わらせる例外であること
- `auto-ok` を付けるのは人だけであること、拾い上げの発火点と順序と 1 ラン 5 件の上限
- ガード（子 30 件 / 自動起票 10 件 / 連続失敗 3 回 / `auto-ok` 5 件）、待機の 4 つの数値、停止条件 6 件
- hand-off の閾値（`pnpm josh cost --over 400000`）と、その報告を完了報告の書式で書かないこと
